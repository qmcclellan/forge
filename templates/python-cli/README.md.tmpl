# {{ project_name }}

Generated by Forge using the `python-cli` template.

## Purpose

{{ description }}

## Setup

    python3 -m venv .venv
    source .venv/bin/activate
    python -m pip install --upgrade pip
    python -m pip install -e ".[dev]"

## Run

    python3 -m {{ package_name }}.cli run

Or, once installed:

    {{ project_slug }} run

## Test

    python -m pytest
